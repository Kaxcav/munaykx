node_modules/
.next/
out/
.env*.local
.env
.vercel
*.tsbuildinfo
next-env.d.ts

# Patches de entrega: chegam na raiz do repo pra serem aplicados e não
# fazem parte do código. Sem esta linha, um `git add -A` depois do
# `git apply` commita o próprio patch junto.
*.patch

# Environment & Secrets (AIOX)
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
